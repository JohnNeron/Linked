<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
  <div class="container">
    <%= link_to "Linked", root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item ">
          <li><%= link_to "Inicio", root_path, class:"nav-link color" %> </li>
          <% if luser_signed_in? %>
        <li><%= link_to "Publicaciones", lposts_path, class:"nav-link color" %> </li>
        <% if Luser.first == current_luser %>
        <li><%= link_to "Usuarios", lusers_path, class:"nav-link color" %> </li>
        <% end %>
        <% if Luser.first != current_luser %>
        <li><%= link_to 'Postulaciones', my_lpostulations_path, class: "nav-link color" %> </li>
          <% end %>
        <% end %>
        </ul>
        <ul class="navbar-nav ms-auto ">
        <li class="nav-item dropdown ">
          <a class="nav-link dropdown-toggle tcolor text-end" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            
            <% if luser_signed_in? %>
            <% if current_luser.name %>
            Hola <%= current_luser.name %>  
            <% else %>
            Hola <%= current_luser.email %>
            <% end %>
            <% if current_luser.avatar.persisted? %>
            <% end %>
            <% else %>
            Inicia sesión
            <% end %>
          </a>
          <% if luser_signed_in? %>
            <ul class="dropdown-menu pff">
              <li><%= button_to "Cerrar Sesión", destroy_luser_session_path, class:"dropdown-item", method: :delete %></li>
            </ul>
            <%= render 'layouts/notifications/notifications' %>
          <% else %> 
          <ul class="dropdown-menu">
            <li><%= link_to "Iniciar Sesión", new_luser_session_path, class:"dropdown-item" %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>